package luisf.ouroboros.visualizer.scene;

import remixlab.proscene.MouseAgent;
import remixlab.proscene.Scene;

public class CameraControlAgent extends MouseAgent {
    public CameraControlAgent(Scene scene) {
        super(scene);
    }
}
